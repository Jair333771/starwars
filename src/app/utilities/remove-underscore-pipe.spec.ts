import { RemoveUnderscorePipe } from './remove-underscore-pipe';

describe('RemoveUnderscorePipe', () => {
  it('should create an instance', () => {
    expect(new RemoveUnderscorePipe()).toBeTruthy();
  });
});
